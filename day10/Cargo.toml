[package]
name = "day10"
version = "0.1.0"
edition = "2021"

[dependencies]
tinyvec = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
